export interface IConfig {
  id: number;
  sensor_interval: number;
  dateformat: string;
  timeformat: string;
  weekformat: string;
  monthformat: string;
  iso_week_format: string;
  use_sensor: boolean;
}
